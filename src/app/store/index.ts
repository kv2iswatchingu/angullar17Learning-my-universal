/* import { NgModule } from "@angular/core";
import { StoreModule } from "@ngrx/store";
import { counterReducer, reducer } from "./reducers/player.reducer";

@NgModule({
    declarations:[],
    imports:[
        StoreModule.forRoot(
            { layer:counterReducer }
        )
    ],
    providers: [],
})
export class storePlayerModule{

} */